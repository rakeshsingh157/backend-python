services:
  - type: web
    name: python-special-backend
    env: python
    region: oregon
    plan: free
    buildCommand: |-
      echo "===================="
      echo "Python Version Check"
      echo "===================="
      python --version
      python3 --version || echo "python3 not available"
      which python
      echo "===================="
      echo "Environment Variables"
      echo "===================="
      env | grep PYTHON
      echo "===================="
      echo "Installing requirements..."
      echo "===================="
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: gunicorn app:app --workers=2 --threads=8 --timeout=300
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: "3.10.14"ype: web
    name: python-special-backend
    env: python
    region: oregon
    plan: free
    buildCommand: |
      echo "Requested Python version: 3.10.1"
      python --version
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: gunicorn app:app --workers=2 --threads=8 --timeout=300
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
      # AI API Keys - Set these in Render dashboard
      - key: GOOGLE_GEMINI_API_KEY
        sync: false
      - key: COHERE_API_KEY
        sync: false
      - key: GROQ_API_KEY
        sync: false
      # Database Configuration - Set these in Render dashboard
      - key: DB_HOST
        sync: false
      - key: DB_USER
        sync: false
      - key: DB_PASSWORD
        sync: false
      - key: DB_DATABASE
        sync: false
      # Flask Configuration
      - key: FLASK_SECRET_KEY
        sync: false
      - key: FLASK_ENV
        value: production
      - key: DEBUG
        value: "False"
      - key: USE_PURE
        value: "True"

